import React, { useEffect } from 'react';
import PropTypes from 'prop-types';

const AccomodationNextArrow = (className, style, onClick) => (
  <div>oui</div>
);

export default AccomodationNextArrow;
