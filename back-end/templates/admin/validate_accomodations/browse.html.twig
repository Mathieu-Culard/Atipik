{% extends 'admin/base.html.twig' %}

{% block title %}Hébergements en attente de validation{% endblock %}

{% block body %}

{{parent()}}

<div class="rightSide">

<main>
<div class="title">
	<h1 class="typeTitleList" id="titleList">Validation des nouveaux hébergements</h1>
</div>

<table class="table table-hover">
		<thead>
			<tr class="table-primary">
				<th scope="col">ID</th>
{# 				<th scope="col">Image principale</th> #}
				<th scope="col">Nom</th>
				<th scope="col">Type</th>
				<th scope="col">Pays</th>
                <th scope="col">Ville</th>
                <th scope="col">Inspecter</th>
             
			</tr>
		</thead>
		<tbody>
<tr>
 
				{% for accomodation in accomodations %}
        <th class="align-middle" scope="row">{{accomodation.id}}</th>
       
{# 		<td>{% for currentPicture in accomodation.picture %}
				<img
				src="{{asset('assets/accomodation/'~ currentPicture.name)}}" alt="">
		
			{% endfor %}</td> #}
        <td class="align-middle">{{accomodation.title}}</td>
        <td class="align-middle">{{accomodation.type.name}}</td>
        <td class="align-middle">{{accomodation.country}}</td>
        <td class="align-middle">{{accomodation.city}}</td>
        <td class="align-middle">

        <a href="{{path('admin_validate_accomodations_read',{'id':accomodation.id}) }}">Inspecter</td>
 
        	</tr>
				{% endfor %}
       

					

</main>
</div>











{% endblock %}
